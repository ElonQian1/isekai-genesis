# 🔄 数据流转图

> **此文件说明数据如何在各模块间流转**

---

## 📊 单人战斗数据流 (本地 WASM)

```
用户操作: 点击卡牌 → 选择目标 → 确认出牌

┌─────────────────────────────────────────────────────────────┐
│  Client (TypeScript)                                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. cl_scene_battle.ts                                      │
│     │                                                       │
│     │ 用户点击卡牌                                           │
│     ▼                                                       │
│  2. cl_entity_card.ts                                       │
│     │ onClick(cardId, targetId)                             │
│     │                                                       │
│     │ 构造请求                                               │
│     ▼                                                       │
│  3. cl_wasm_bridge.ts                                       │
│     │                                                       │
│     │ clWasmPlayCard(playerId, cardId, targetId)           │
│     │                                                       │
│     ▼                                                       │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  WASM (game-wasm)                                    │   │
│  │                                                      │   │
│  │  4. gw_bridge.rs                                     │   │
│  │     │ gw_play_card(player_id, card_id, target_id)   │   │
│  │     │                                               │   │
│  │     │ 解析 JSON，调用 game-core                      │   │
│  │     ▼                                               │   │
│  │  ┌───────────────────────────────────────────────┐  │   │
│  │  │  game-core                                     │  │   │
│  │  │                                                │  │   │
│  │  │  5. gc_validation.rs                           │  │   │
│  │  │     │ gc_validate_play_card(...)               │  │   │
│  │  │     │ → 检查回合、手牌、目标                     │  │   │
│  │  │     │ → Ok(()) 或 Err(GcValidationError)       │  │   │
│  │  │     ▼                                          │  │   │
│  │  │  6. gc_battle.rs                               │  │   │
│  │  │     │ gc_execute_play_card(...)                │  │   │
│  │  │     │ → 计算伤害                                │  │   │
│  │  │     │ → 应用效果                                │  │   │
│  │  │     │ → 更新状态                                │  │   │
│  │  │     │ → 返回 GcPlayCardResult                  │  │   │
│  │  │     ▼                                          │  │   │
│  │  └───────────────────────────────────────────────┘  │   │
│  │     │                                               │   │
│  │     │ 序列化为 JSON                                  │   │
│  │     ▼                                               │   │
│  │  7. 返回 JSON 字符串                                 │   │
│  │                                                      │   │
│  └──────────────────────────────────────────────────────┘   │
│     │                                                       │
│     │ 解析 JSON → GcPlayCardResult                         │
│     ▼                                                       │
│  8. cl_wasm_bridge.ts                                       │
│     │ return result;                                        │
│     ▼                                                       │
│  9. cl_scene_battle.ts                                      │
│     │                                                       │
│     │ if (result.success)                                   │
│     │   playCardAnimation(card, target, result)            │
│     │   updateBattleState(result.new_state)                │
│     │ else                                                  │
│     │   showError(result.error)                            │
│     ▼                                                       │
│  10. Babylon.js 渲染更新                                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 多人战斗数据流 (WebSocket)

```
玩家 A 出牌 → 服务器验证 → 广播给所有玩家

┌────────────────────────────┐     ┌────────────────────────────┐
│       Client A             │     │       Client B             │
│    (出牌的玩家)            │     │    (对手)                  │
└────────────────────────────┘     └────────────────────────────┘
            │                                 ▲
            │ 1. 发送                          │ 6. 接收
            │                                 │
            ▼                                 │
      ┌─────────────────────────────────────────────────────┐
      │  WebSocket 消息                                      │
      │  {                                                   │
      │    "type": "gs_play_card",                          │
      │    "card_id": "card_123",                           │
      │    "target_id": "player_b"                          │
      │  }                                                   │
      └─────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                     game-server (Rust)                      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  2. gs_ws.rs                                                │
│     │ 解析消息                                               │
│     │ 查找玩家会话和房间                                      │
│     ▼                                                       │
│  3. gs_battle.rs                                            │
│     │ GsBattleCoordinator::gs_handle_play_card()           │
│     │                                                       │
│     │ 调用 game-core 验证和执行                              │
│     ▼                                                       │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  game-core (服务器端共享)                            │   │
│  │                                                      │   │
│  │  4. gc_validation.rs                                 │   │
│  │     │ gc_validate_play_card(...)                     │   │
│  │     ▼                                                │   │
│  │     gc_battle.rs                                     │   │
│  │     │ gc_execute_play_card(...)                      │   │
│  │     │ → 返回 GcPlayCardResult                        │   │
│  │                                                      │   │
│  └─────────────────────────────────────────────────────┘   │
│     │                                                       │
│     │ 验证成功                                               │
│     ▼                                                       │
│  5. gs_battle.rs                                            │
│     │ gs_broadcast_to_room(...)                             │
│     │                                                       │
│     │ 广播给房间内所有玩家                                    │
│     ▼                                                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
      ┌─────────────────────────────────────────────────────┐
      │  WebSocket 广播消息                                  │
      │  {                                                   │
      │    "type": "gs_battle_update",                       │
      │    "result": { GcPlayCardResult },                   │
      │    "state": { GcBattleState }                        │
      │  }                                                   │
      └─────────────────────────────────────────────────────┘
            │                                 │
            ▼                                 ▼
┌────────────────────────────┐     ┌────────────────────────────┐
│       Client A             │     │       Client B             │
│  7. 播放自己出牌动画       │     │  7. 播放对手出牌动画       │
│     更新战斗状态           │     │     更新战斗状态           │
└────────────────────────────┘     └────────────────────────────┘
```

---

## 📊 房间系统数据流

```
创建房间 → 加入房间 → 准备 → 开始游戏

时间线 ──────────────────────────────────────────────────────→

玩家 A (房主)                  服务器                    玩家 B
    │                           │                          │
    │  gs_create_room ─────────▶│                          │
    │                           │ 创建房间                  │
    │◀───── gs_room_joined ─────│                          │
    │                           │                          │
    │                           │◀───── gs_join_room ──────│
    │                           │ 加入房间                  │
    │◀──── gs_room_updated ─────│───── gs_room_joined ────▶│
    │      (新玩家加入)         │                          │
    │                           │                          │
    │                           │◀───── gs_ready ──────────│
    │◀──── gs_room_updated ─────│───── gs_room_updated ───▶│
    │      (玩家 B 准备)        │      (玩家 B 准备)       │
    │                           │                          │
    │  gs_ready ───────────────▶│                          │
    │◀──── gs_room_updated ─────│───── gs_room_updated ───▶│
    │      (玩家 A 准备)        │      (玩家 A 准备)       │
    │                           │                          │
    │                           │ 所有人准备，开始游戏       │
    │                           │                          │
    │◀──── gs_battle_start ─────│───── gs_battle_start ───▶│
    │      (战斗状态)           │      (战斗状态)          │
    │                           │                          │
    ▼                           ▼                          ▼
  战斗阶段                   协调战斗                   战斗阶段
```

---

## 📊 类型流转对照表

| 阶段 | 数据位置 | 类型 | 格式 |
|------|----------|------|------|
| 用户交互 | Client | `ClCardClick` | TypeScript 对象 |
| 调用 WASM | Client → WASM | `string` | JSON |
| WASM 处理 | WASM | `GcPlayer, GcCard` | Rust 结构体 |
| 核心计算 | game-core | `GcPlayCardResult` | Rust 结构体 |
| 返回前端 | WASM → Client | `string` | JSON |
| 解析结果 | Client | `GcPlayCardResult` | TypeScript 接口 |
| 发送服务器 | Client → Server | `GsClientMsg` | JSON |
| 服务器处理 | Server | `GcBattleState` | Rust 结构体 |
| 广播客户端 | Server → Client | `GsServerMsg` | JSON |

---

## ⚠️ 关键原则

### 1. 单一数据源
```
战斗状态的真相:
├── 单人游戏: WASM 中的 GcBattleState
└── 多人游戏: 服务器中的 GcBattleState

Client 只是显示层，不持有权威状态
```

### 2. 计算位置
```
所有游戏逻辑计算都在 game-core:
├── WASM 调用 game-core (单人/预览)
└── Server 调用 game-core (多人/验证)

Client 永远不直接计算游戏逻辑
```

### 3. 验证顺序
```
多人游戏的出牌:
1. Client 可以先调用 WASM 预验证 (可选，提升体验)
2. 发送到 Server
3. Server 用 game-core 再次验证 (权威)
4. 验证通过才广播
```

---

## ⚠️ AI 代理注意事项

1. **逻辑计算只在 game-core** - 其他模块只是调用
2. **JSON 是跨边界格式** - WASM ↔ JS, WebSocket
3. **服务器是多人游戏的权威** - 客户端不信任
4. **理解数据流向** - 修改代码前先确认流程
5. **保持类型同步** - Rust 改了 TypeScript 也要改
