# 游戏服务器环境变量配置示例
# 
# 使用方法:
# 1. 复制此文件为 .env
# 2. 根据实际情况修改配置值
# 3. 确保 .env 文件不被提交到版本控制

# =============================================================================
# 数据库配置
# =============================================================================

# PostgreSQL 数据库连接字符串
# 格式: postgres://用户名:密码@主机:端口/数据库名
DATABASE_URL=postgres://card_game:your_password_here@localhost:5432/card_game_db

# =============================================================================
# 认证配置
# =============================================================================

# JWT 密钥 (用于签署和验证登录令牌)
# ⚠️ 生产环境请使用强随机密钥 (至少 32 字符)
# 可使用以下命令生成: openssl rand -hex 32
JWT_SECRET=your-super-secret-key-change-in-production

# JWT 令牌有效期 (小时)
JWT_EXPIRY_HOURS=24

# =============================================================================
# 服务器配置
# =============================================================================

# HTTP 服务端口
PORT=3000

# 服务器绑定地址 (0.0.0.0 允许外部访问)
HOST=0.0.0.0

# 运行环境: development | production
RUST_ENV=development

# =============================================================================
# 日志配置
# =============================================================================

# 日志文件保存目录
# 本地开发: 文档/日志 (中文路径，Windows 友好)
# 生产环境: /var/log/card-game 或 logs
LOG_DIR=logs

# 启动时是否清空日志 (development 设为 true, production 设为 false)
CLEAR_LOGS=true

# Rust 日志级别
RUST_LOG=info,game_server=debug

# 日志级别: trace | debug | info | warn | error
RUST_LOG=game_server=info,tower_http=debug

# =============================================================================
# WebSocket 配置
# =============================================================================

# WebSocket 最大连接数
WS_MAX_CONNECTIONS=1000

# WebSocket 心跳间隔 (秒)
WS_HEARTBEAT_INTERVAL=30

# =============================================================================
# 游戏配置
# =============================================================================

# 最大房间人数
MAX_ROOM_PLAYERS=4

# 战斗回合超时 (秒)
BATTLE_TURN_TIMEOUT=60

# =============================================================================
# CORS 配置 (跨域)
# =============================================================================

# 允许的前端域名 (多个用逗号分隔)
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://114.132.81.233
