# =============================================================================
# 🦀 game-core - 共享核心逻辑
# =============================================================================
# 模块前缀: gc_ / Gc
# 编译目标: WASM + Native (双端共享)
# 文档: 文档/01-game-core.md
# =============================================================================

[package]
name = "game-core"
version = "0.1.0"
edition = "2021"
description = "游戏核心逻辑 - 前后端共享"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
uuid = { workspace = true }

[dev-dependencies]
