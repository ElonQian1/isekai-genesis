# 🎮 精美 3D 网页卡牌游戏

> ⚠️ **AI 代理请先阅读 [AGENTS.md](./AGENTS.md) 和 [.github/copilot-instructions.md](./.github/copilot-instructions.md)**

---

## 🚀 技术栈 (正在迁移)

```
┌─────────────────────────────────────────────────────────────┐
│  🎨 前端：Babylon.js + WebGPU + TypeScript                  │
│  🦀 逻辑：Rust → WASM (前端) + Native (后端) 共享           │
│  🦀 后端：Rust + Axum + Tokio + PostgreSQL                  │
└─────────────────────────────────────────────────────────────┘
```

---

## 📁 项目结构

### 新结构 (目标)
```
├── Cargo.toml              # Rust Workspace
├── crates/
│   ├── game-core/          # 🦀 共享核心逻辑
│   ├── game-wasm/          # 🦀 WASM 绑定
│   └── game-server/        # 🦀 Rust 后端
├── client/                 # 🎨 前端 (Babylon.js + WebGPU)
└── assets/                 # 美术资源
```

### 旧结构 (待迁移)
```
├── packages/
│   ├── client/             # 旧前端 (React)
│   ├── server/             # 旧后端 (Node.js)
│   └── shared/             # 旧共享类型
```

---

## 🔧 开发命令

### Rust (新)
```bash
# 编译 WASM
cd crates/game-wasm && wasm-pack build --target web

# 运行服务器
cd crates/game-server && cargo run

# 测试
cargo test --workspace
```

### 前端 (新)
```bash
cd client && pnpm dev
```

### 旧版 (临时)
```bash
npm run dev:server
npm run dev:client
```

---

## 🌐 在线访问

http://114.132.81.233
