# AI 代理项目指南

> ⚠️ **所有 AI 代理必须阅读此文件**

---

## 📚 必读文档

| 文档 | 内容 | 何时阅读 |
|------|------|----------|
| [00-项目总览.md](./文档/00-项目总览.md) | 技术栈、模块概览 | **首次必读** |
| [05-命名规范.md](./文档/05-命名规范.md) | 命名前缀系统 | **首次必读** |
| [01-game-core.md](./文档/01-game-core.md) | 共享核心模块 | 改 game-core 时 |
| [02-game-wasm.md](./文档/02-game-wasm.md) | WASM 绑定层 | 改 game-wasm 时 |
| [03-game-server.md](./文档/03-game-server.md) | 后端服务器 | 改 game-server 时 |
| [04-client.md](./文档/04-client.md) | 前端渲染 | 改 client 时 |
| [06-API接口.md](./文档/06-API接口.md) | 接口定义 | 跨模块通信时 |
| [07-数据流转.md](./文档/07-数据流转.md) | 数据流图 | 理解架构时 |

---

## 🎯 项目概述

**精美 3D 网页卡牌游戏** - 采用 Rust + WebGPU 高性能技术栈

---

## 🛠️ 核心技术栈

```
┌─────────────────────────────────────────────────────────────┐
│  🎨 前端：Babylon.js + WebGPU + TypeScript                  │
│  🦀 逻辑：Rust → WASM (前端) + Native (后端) 共享           │
│  🦀 后端：Rust + Axum + Tokio + PostgreSQL                  │
└─────────────────────────────────────────────────────────────┘
```

| 层级 | 技术 | 文件位置 |
|------|------|----------|
| 3D 渲染 | Babylon.js + WebGPU | `client/` |
| 游戏逻辑 | Rust WASM | `crates/game-wasm/` |
| 共享核心 | Rust | `crates/game-core/` |
| 后端服务 | Rust Axum | `crates/game-server/` |

---

## 📁 项目结构

```
项目根目录/
├── Cargo.toml              # Rust Workspace 配置
├── crates/
│   ├── game-core/          # 🦀 共享核心 (前后端复用)
│   ├── game-wasm/          # 🦀 WASM 导出
│   └── game-server/        # 🦀 Rust 后端
├── client/                 # 🎨 前端 Babylon.js
└── assets/                 # 美术资源
```

---

## ⚠️ 重要规则

### ✅ 必须做
- 使用 **Rust** 编写后端和游戏逻辑
- 使用 **Babylon.js + WebGPU** 渲染 3D
- 使用 **TypeScript** 编写前端代码
- 在 `game-core` 中实现共享逻辑
- 使用 **serde_json** 在 WASM 和 JS 间传数据

### ❌ 禁止做
- 不要使用 Node.js 后端 (迁移到 Rust)
- 不要使用 React 渲染 3D
- 不要使用 JavaScript (用 TypeScript)
- 不要使用 `.unwrap()` (用 `?` 或 `match`)
- 不要使用 `any` 类型

---

## 🔧 常用命令

```bash
# Rust WASM 编译
cd crates/game-wasm && wasm-pack build --target web

# Rust 服务器
cd crates/game-server && cargo run

# 前端开发
cd client && pnpm dev

# 测试
cargo test --workspace
```

---

## 🌐 服务器信息

| 项目 | 值 |
|------|-----|
| IP 地址 | `114.132.81.233` |
| 用户名 | `ubuntu` |
| 认证方式 | SSH 密钥（无需密码） |
| 项目路径 | `/var/www/card-game` |
| 游戏地址 | http://114.132.81.233 |

```bash
# SSH 连接
ssh ubuntu@114.132.81.233

# 查看服务状态  
ssh ubuntu@114.132.81.233 "pm2 status"
```

---

## 📋 开发阶段

当前阶段：**技术栈迁移中**

- [ ] 创建 Rust Workspace
- [ ] 实现 game-core 共享逻辑
- [ ] 实现 game-wasm 前端绑定
- [ ] 实现 game-server Rust 后端
- [ ] 前端迁移到 Babylon.js + WebGPU
- [ ] 完成 3D 渲染和特效
